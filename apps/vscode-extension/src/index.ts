/**
 * Composition root for the Agentix OS VS Code extension.
 *
 * Phase 0 provides only the buildable skeleton. The extension host
 * (activate/deactivate), webview, IPC layer, and dependency-injection
 * wiring of runtime + agents + AI providers are implemented in Phase 1+.
 */
export const EXTENSION_PACKAGE = "@agentix/vscode-extension";
