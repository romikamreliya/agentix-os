export const SHARED_PACKAGE = "@agentix/shared";

/**
 * Foundation package version marker. Real utilities (logger, errors,
 * validation, config, IPC types) are added in later phases.
 */
export const VERSION = "0.0.0";
