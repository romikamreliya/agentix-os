export const AGENTS_PACKAGE = "@agentix/agents";

/**
 * Parent Agent, Agent Registry, and specialized agents (Discovery,
 * Research, Planning, Review, Knowledge) are added from Phase 5 onward.
 * Agents call AI providers through IAIProvider in @agentix/core; they do
 * not import @agentix/ai directly.
 */
